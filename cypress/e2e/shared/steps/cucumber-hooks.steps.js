import { Before } from '@badeball/cypress-cucumber-preprocessor';

// This import is critical, only with this '@badeball/cypress-cucumber-preprocessor' can process custom parameters
import '../../../support/parameter_type';

// Placeholder - 'Before' hook gets executed before each scenario
Before(() => {});